/*
 * napi_bindings.cc — ARQUIVO DESCONTINUADO / NÃO COMPILADO
 *
 * Este arquivo era uma duplicata de audio_capture.cc e NÃO é compilado
 * em nenhuma plataforma (binding.gyp usa audio_capture.cc no Win32
 * e stub.cc em outros sistemas).
 *
 * Todo o código N-API (StartCapture, StopCapture, SetAudioCallback, etc.)
 * está definido em audio_capture.cc, que é o único arquivo relevante.
 *
 * Este arquivo é mantido vazio para preservar o histórico Git e evitar
 * confusão com o binding.gyp que não o referencia.
 */